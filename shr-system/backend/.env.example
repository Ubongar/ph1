# Backend runtime
PORT=8787

# Required in production: change to a long random secret
SHR_SYNC_TOKEN_SECRET=replace-with-a-long-random-secret

# Token lifetime in seconds
SHR_SYNC_TOKEN_TTL_SECONDS=3600

# Optional: if you add persistent DB later
# DATABASE_URL=
